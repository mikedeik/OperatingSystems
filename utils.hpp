#include <ctime>
#include <random>
#include <iostream>

#ifndef RANDOMEXP
#define RANDOMEXP



// Returns exp distributed random number
double RandExp(int random , double l);

// Calculates outcome based on probability
bool calculate_outcome(const float probability);

#endif // RANDOMEXP